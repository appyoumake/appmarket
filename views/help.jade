extends layout

block content
  h1= title
  p Welcome to #{title}

  each user in users
    li
      = user.email
      | &nbsp;
      | user.id
      each t in user.Device
        li
          = device.os
